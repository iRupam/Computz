#include<iostream.h>
#include<conio.h>
void main()
{
      clrscr();
      int a,b;
      cout<<"\nEnter any two nos.:";
      cin>>a>>b;

      if(a>b)
      {
	    cout<<a<<"is the largest no.";
      }
      else
      {
	    cout<<b<< "is the largest no.";
      }
      getch();
      }